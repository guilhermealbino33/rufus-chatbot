# DATABASE
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=rufus_chatbot

NODE_ENV=development
PORT=3000

# SERVER (abstração: trocar provedor só alterando env)
# CORS_ORIGIN=* ou lista separada por vírgula (vazio = allow all)
# CORS_ORIGIN=https://app.example.com,https://admin.example.com

# WHATSAPP
WHATSAPP_TEST_PHONE_NUMBER=5511999999999

# OPCIONAIS (produção / Railway)
# WA_SESSION_TOKEN=          # placeholder futura auth/sessão
# JWT_SECRET=                 # secret para JWT (AuthModule)
# CHROMIUM_EXECUTABLE_PATH=   # ex: /usr/bin/chromium (Docker); omitir em dev