# Dependencies (reinstalled in container)
node_modules
npm-debug.log*

# Build output (rebuilt in container)
dist
coverage
*.tsbuildinfo

# Test and tooling
test
*.spec.ts
.eslintcache
.env
.env.*
!.env.example

# Git and IDE
.git
.gitignore
.dockerignore
Dockerfile*
*.md

# WPP session data (ephemeral)
wpp-sessions
